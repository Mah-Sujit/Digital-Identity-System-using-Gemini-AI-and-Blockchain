{
  "name": "digital-identity-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && nodemon src/server.js",
    "frontend": "cd frontend && npm run dev",
    "blockchain": "cd blockchain && npx hardhat node",
    "dev": "concurrently \"npm run blockchain\" \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}